# .env (добавьте в .gitignore!)
POSTGRES_DB = pizzasDb
POSTGRES_USER = myuser
POSTGRES_PASSWORD = mypassword
DATABASE_URL = postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}